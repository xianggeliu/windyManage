
spring:
  profiles:
    active: prod
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url:  jdbc:mysql://127.0.0.1:3306/windy
    data-username: root
    data-password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true